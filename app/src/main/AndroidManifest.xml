<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.hixel.hixel">

  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.CAMERA"/>

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:name=".App"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity
      android:name=".dashboard.DashboardActivity"
      android:windowSoftInputMode="adjustNothing">
    </activity>
    <activity android:name=".companydetail.CompanyDetailActivity">
    </activity>
    <activity
      android:name=".companycomparison.CompanyComparisonActivity"
      android:windowSoftInputMode="stateHidden">
    </activity>
    <!-- suppress AndroidDomInspection -->
    <!-- <activity android:name=".comparisonGraph.MainActivity"/> -->
    <activity android:name=".companycomparison.GraphActivity"
      android:windowSoftInputMode="adjustNothing">

    </activity>
    <!-- suppress AndroidDomInspection -->
    <!-- <activity android:name=".comparisonGraph.MainActivity"/> -->
    <meta-data
      android:name="preloaded_fonts"
      android:resource="@array/preloaded_fonts"/>

    <activity
      android:name=".login.LoginActivity"
      android:windowSoftInputMode="stateHidden">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity
      android:name=".login.SignupActivity"
      android:windowSoftInputMode="stateHidden">
    </activity>

    <activity android:name=".login.Onboarding"/>

    <activity android:name=".login.ForgotPasswordActivity"/>

    <activity android:name=".login.ProfileActivity" />

    <activity android:name=".login.PinInputActivity" />

    <activity android:name=".login.UpdatePasswordActivity" />

  </application>
</manifest>