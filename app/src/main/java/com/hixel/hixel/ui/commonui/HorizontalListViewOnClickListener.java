package com.hixel.hixel.ui.commonui;

/**
 * OnClick interface to listen for on click events between different view components.
 */
public interface HorizontalListViewOnClickListener {
    void onClick(String ratio);
}
